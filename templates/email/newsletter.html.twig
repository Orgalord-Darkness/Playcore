<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <title>Newsletter Gaming</title>
    {# <link href="/public/assets/css/style.css" rel="stylesheet"></link> #}
    <style>
      body {
          font-family: 'Segoe UI', Roboto, sans-serif;
          background-color: #1e1e2f;
          color: #ffffff;
          margin: 0;
          padding: 0;
      }
      .container {
          max-width: 600px;
          margin: 40px auto;
          background-color: #2a2a3d;
          border-radius: 8px;
          overflow: hidden;
          box-shadow: 0 0 10px rgba(0,0,0,0.3);
      }
      .header {
          background-color: #ff4655;
          padding: 20px;
          text-align: center;
      }
      .header h1 {
          margin: 0;
          font-size: 26px;
          color: #fff;
      }
      .content {
          padding: 30px;
      }
      .content h2 {
          color: #ff4655;
          font-size: 20px;
      }
      .content p {
          line-height: 1.6;
          font-size: 15px;
      }
      .game-highlight {
          background-color: #3a3a4f;
          padding: 15px;
          border-radius: 6px;
          margin-top: 20px;
      }
      .game-highlight h3 {
          margin-top: 0;
          color: #ffd700;
      }
      .button {
          display: inline-block;
          margin-top: 20px;
          padding: 12px 20px;
          background-color: #ff4655;
          color: white;
          text-decoration: none;
          border-radius: 4px;
          font-weight: bold;
      }
      .footer {
          background-color: #1e1e2f;
          text-align: center;
          padding: 15px;
          font-size: 12px;
          color: #aaa;
      }
    </style>
  </head>
  <body>
  <div class="container">
    <div class="header">
      <h1>🔥 Gaming News - Octobre 2025</h1>
    </div>
    {% for game in videoGames %}
      <div class="game-highlight">
        {% if game.coverImage %}
          <img src="{{ asset('assets/cover/' ~ game.coverImage) }}" 
            alt="Image de {{ game.title }}" style="max-width: 300px; height: auto;"
            style="width: 100%; 
              max-width: 400px; 
              height: 400px; 
              display: block; 
              margin-bottom: 1em; 
              border-radius: 8px;" 
           />
        {% endif %}
        
        <h3>🎮 {{ game.title }}</h3>
        <p>{{ game.description }}</p>
        <p><strong>Date de sortie :</strong> {{ game.releaseDate|date('d/m/Y') }}</p>
        {% if game.editor %}
          <p><strong>Éditeur :</strong> {{ game.editor.name }}</p>
        {% endif %}
    </div>
    {% else %}
      <p>Aucun jeu vidéo récent à afficher pour le moment.</p>
    {% endfor %}
  </body>
</html>
